const toast=document.querySelector(".toast");document.querySelectorAll(".order").forEach(b=>b.addEventListener("click",()=>show(`"${b.dataset.item}" selected. We'll connect ordering and PayPal next.`)));document.querySelector("#quoteForm")?.addEventListener("submit",e=>{e.preventDefault();show("Quote request prepared. We'll connect the form to email next.");e.target.reset()});document.querySelector("#contactForm")?.addEventListener("submit",e=>{e.preventDefault();show("Message prepared. We'll connect the contact form to email next.");e.target.reset()});const top=document.querySelector(".top");addEventListener("scroll",()=>top.classList.toggle("show",scrollY>500));top?.addEventListener("click",()=>scrollTo({top:0,behavior:"smooth"}));function show(t){toast.textContent=t;toast.classList.add("show");clearTimeout(window.t);window.t=setTimeout(()=>toast.classList.remove("show"),3500)}